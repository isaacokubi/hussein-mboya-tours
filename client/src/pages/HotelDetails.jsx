import { Link, useParams } from "react-router-dom";
import { useQuery } from "@tanstack/react-query";
import { ArrowLeft, BedDouble, Building2, CheckCircle2, Clock3, Coffee, CreditCard, MapPin, ShieldCheck, Star, Users } from "lucide-react";
import { getHotel } from "../api/hotelApi";

const money = (value, currency = "KES") => `${currency} ${Number(value || 0).toLocaleString("en-KE", { maximumFractionDigits: 2 })}`;
const meal = value => String(value || "room_only").replaceAll("_", " ").replace(/\b\w/g, x => x.toUpperCase());

export default function HotelDetails() {
  const { id } = useParams();
  const { data: hotel, isLoading, isError } = useQuery({ queryKey: ["public-hotel", id], queryFn: () => getHotel(id), enabled: !!id, staleTime: 30000 });
  if (isLoading) return <main className="min-h-screen bg-slate-50 px-5 py-16"><div className="mx-auto max-w-6xl animate-pulse space-y-6"><div className="h-80 rounded-3xl bg-slate-200"/><div className="h-10 w-1/2 rounded bg-slate-200"/><div className="h-32 rounded-3xl bg-slate-200"/></div></main>;
  if (isError || !hotel) return <main className="min-h-screen bg-slate-50 px-5 py-20"><div className="mx-auto max-w-xl rounded-3xl bg-white p-10 text-center shadow-sm ring-1 ring-slate-200"><Building2 className="mx-auto text-slate-300" size={52}/><h1 className="mt-5 text-2xl font-black text-slate-900">Hotel not available</h1><p className="mt-2 text-sm leading-6 text-slate-500">This property may have been removed, is temporarily unavailable, or the link is no longer valid.</p><Link to="/hotels" className="mt-6 inline-flex rounded-xl bg-slate-950 px-5 py-3 text-sm font-black text-white">Back to hotels</Link></div></main>;
  const rooms = hotel.roomTypes || [];
  const image = hotel.images?.[0];
  const location = hotel.location || [hotel.city, hotel.county].filter(Boolean).join(", ") || "Kenya";
  return <main className="min-h-screen bg-slate-50"><section className="bg-slate-950 text-white"><div className="mx-auto max-w-7xl px-5 py-5 md:px-8"><Link to="/hotels" className="inline-flex items-center gap-2 text-sm font-bold text-slate-300 hover:text-white"><ArrowLeft size={17}/> Back to hotels</Link></div></section><section className="mx-auto max-w-7xl px-5 py-7 md:px-8 md:py-10"><div className="overflow-hidden rounded-[2rem] bg-white shadow-sm ring-1 ring-slate-200"><div className="grid lg:grid-cols-[1.35fr_.65fr]">{image ? <img src={image} alt={hotel.name} className="h-72 w-full object-cover md:h-96 lg:h-full"/> : <div className="flex min-h-72 items-center justify-center bg-gradient-to-br from-slate-900 to-emerald-900 text-white"><Building2 size={76}/></div>}<div className="p-7 md:p-9"><div className="flex flex-wrap items-center gap-2"><span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-black uppercase tracking-wider text-emerald-700">Verified accommodation inventory</span>{hotel.starRating ? <span className="inline-flex items-center gap-1 rounded-full bg-amber-50 px-3 py-1 text-xs font-black text-amber-700"><Star size={13} className="fill-current"/> {hotel.starRating} star</span> : null}</div><h1 className="mt-5 text-3xl font-black tracking-tight text-slate-950 md:text-5xl">{hotel.name}</h1><p className="mt-3 flex items-center gap-2 text-sm font-semibold text-slate-500"><MapPin size={17} className="text-emerald-600"/>{location}</p><p className="mt-6 text-sm leading-7 text-slate-600">{hotel.description || "Comfortable accommodation for your Kenyan itinerary, with clear room rates and a reservation workflow designed for reliable guest operations."}</p><div className="mt-7 grid grid-cols-2 gap-3"><Info icon={<Clock3/>} label="Check-in" value={hotel.checkInTime || "14:00"}/><Info icon={<Clock3/>} label="Check-out" value={hotel.checkOutTime || "11:00"}/><Info icon={<BedDouble/>} label="Room types" value={String(rooms.length)}/><Info icon={<CreditCard/>} label="Currency" value={hotel.currency || "KES"}/></div></div></div></div></section><section className="mx-auto max-w-7xl px-5 pb-16 md:px-8"><div className="grid gap-7 lg:grid-cols-[1fr_340px]"><div><div className="mb-5"><p className="text-sm font-black uppercase tracking-wider text-emerald-700">Accommodation options</p><h2 className="mt-1 text-3xl font-black text-slate-950">Choose your room</h2><p className="mt-2 text-sm text-slate-500">Rates are shown in Kenyan shillings where configured. Availability is checked again during reservation.</p></div><div className="space-y-4">{rooms.map(room => <article key={room._id} className="rounded-3xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><div className="flex flex-col gap-5 md:flex-row md:items-center md:justify-between"><div><h3 className="text-xl font-black text-slate-950">{room.name}</h3><div className="mt-2 flex flex-wrap gap-3 text-xs text-slate-500"><span className="inline-flex items-center gap-1"><Users size={14}/> Up to {room.maxAdults || 1} adults</span><span className="inline-flex items-center gap-1"><BedDouble size={14}/> {room.availableRooms ?? room.totalRooms ?? 0} available</span><span className="inline-flex items-center gap-1"><Coffee size={14}/> {(room.mealPlans || ["room_only"]).map(meal).join(" · ")}</span></div></div><div className="md:text-right"><p className="text-2xl font-black text-slate-950">{money(room.nightlyRate, room.currency || hotel.currency)}</p><p className="text-xs text-slate-400">per room / night</p><Link to="/hotels" className="mt-3 inline-flex rounded-xl bg-emerald-600 px-5 py-3 text-sm font-black text-white hover:bg-emerald-500">Choose dates & reserve</Link></div></div></article>)}{!rooms.length && <div className="rounded-3xl border border-dashed border-slate-300 bg-white p-12 text-center text-sm text-slate-500">No active room types are currently published for this property.</div>}</div></div><aside className="space-y-4"><div className="rounded-3xl bg-slate-950 p-6 text-white"><p className="text-xs font-black uppercase tracking-wider text-emerald-300">Kenya-ready booking</p><h3 className="mt-2 text-xl font-black">Clear rates. Confirmed inventory.</h3><ul className="mt-5 space-y-3 text-sm text-slate-300"><li className="flex gap-2"><CheckCircle2 className="shrink-0 text-emerald-400" size={17}/> Live availability checks</li><li className="flex gap-2"><CheckCircle2 className="shrink-0 text-emerald-400" size={17}/> KES pricing for local guests</li><li className="flex gap-2"><CheckCircle2 className="shrink-0 text-emerald-400" size={17}/> Secure reservation workflow</li><li className="flex gap-2"><CheckCircle2 className="shrink-0 text-emerald-400" size={17}/> Guest details captured for operations</li></ul></div><div className="rounded-3xl bg-white p-6 ring-1 ring-slate-200"><div className="flex items-center gap-3"><ShieldCheck className="text-emerald-600"/><div><p className="font-black text-slate-950">Travel with confidence</p><p className="text-xs text-slate-500">Reservation details are handled through the platform's protected workflow.</p></div></div><div className="mt-5 rounded-2xl bg-slate-50 p-4 text-xs leading-5 text-slate-600">Need an airport pickup too? Pair this stay with a pre-booked airport transfer and provide your flight details to the operations team.</div><Link to="/airport-transfers" className="mt-4 inline-flex w-full justify-center rounded-xl border border-slate-200 px-4 py-3 text-sm font-black text-slate-900 hover:border-emerald-300">Browse airport transfers</Link></div></aside></div></section></main>;
}
function Info({ icon, label, value }) { return <div className="rounded-2xl bg-slate-50 p-4"><div className="flex items-center gap-2 text-emerald-600">{icon}</div><p className="mt-2 text-[10px] font-black uppercase tracking-wider text-slate-400">{label}</p><p className="mt-1 text-sm font-black text-slate-900">{value}</p></div>; }

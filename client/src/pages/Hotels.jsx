import { useMemo, useState } from "react";
import { Link } from "react-router-dom";
import { useMutation, useQuery } from "@tanstack/react-query";
import { Building2, BedDouble, CalendarDays, Check, MapPin, Search, ShieldCheck, Star, Users, X } from "lucide-react";
import { createHotelBooking, getHotels } from "../api/hotelApi";

const input = "mt-1 w-full rounded-xl border border-slate-200 bg-white px-3.5 py-3 text-sm text-slate-900 outline-none transition focus:border-emerald-500 focus:ring-4 focus:ring-emerald-500/10";
const field = (label, control) => <label className="text-sm font-medium text-slate-700">{label}{control}</label>;

export default function Hotels() {
  const { data = [], isLoading, isError } = useQuery({ queryKey: ["public-hotels"], queryFn: getHotels });
  const [selected, setSelected] = useState(null); const [search, setSearch] = useState("");
  const [form, setForm] = useState({ checkIn: "", checkOut: "", rooms: 1, adults: 1, children: 0, firstName: "", lastName: "", phone: "", email: "", mealPlan: "room_only", specialRequests: "" });
  const update = (key, value) => setForm(f => ({ ...f, [key]: value }));
  const filtered = useMemo(() => data.filter(h => `${h.name} ${h.location || ""} ${h.city || ""} ${h.county || ""}`.toLowerCase().includes(search.toLowerCase())), [data, search]);
  const booking = useMutation({ mutationFn: createHotelBooking, onSuccess: d => { setSelected(null); window.alert(`Hotel booking ${d.reference} has been requested. Our team will confirm availability and payment.`); }, onError: e => window.alert(e?.response?.data?.message || "Unable to create the hotel reservation. Please sign in and try again.") });
  const choose = (hotel, room) => { setSelected({ hotel, room }); update("adults", Math.min(Number(form.adults) || 1, room.maxAdults || 2)); };
  return <main className="min-h-screen bg-slate-50">
    <section className="relative overflow-hidden bg-slate-950 text-white"><div className="absolute inset-0 bg-[radial-gradient(circle_at_20%_20%,rgba(16,185,129,.28),transparent_38%),radial-gradient(circle_at_80%_10%,rgba(59,130,246,.2),transparent_32%)]"/><div className="relative mx-auto max-w-7xl px-5 py-16 md:px-8 md:py-20"><div className="max-w-3xl"><div className="inline-flex items-center gap-2 rounded-full border border-emerald-400/30 bg-emerald-400/10 px-3 py-1.5 text-xs font-bold uppercase tracking-[.16em] text-emerald-300"><Building2 size={14}/> Hotels & accommodation</div><h1 className="mt-5 text-4xl font-black tracking-tight md:text-6xl">Stay somewhere worth remembering.</h1><p className="mt-5 max-w-2xl text-base leading-7 text-slate-300 md:text-lg">Discover quality accommodation for your Kenya itinerary, with transparent KES rates and room options designed for couples, families and groups.</p></div><div className="mt-9 flex max-w-2xl items-center gap-2 rounded-2xl border border-white/10 bg-white/10 p-2 backdrop-blur"><Search className="ml-3 text-slate-400" size={20}/><input value={search} onChange={e => setSearch(e.target.value)} placeholder="Search hotel, city or county" className="min-w-0 flex-1 bg-transparent px-2 py-3 text-white outline-none placeholder:text-slate-500"/></div></div></section>
    <section className="mx-auto max-w-7xl px-5 py-10 md:px-8"><div className="mb-7 flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-bold uppercase tracking-wider text-emerald-700">Curated stays</p><h2 className="mt-1 text-3xl font-black text-slate-900">Hotels for your journey</h2></div><div className="flex items-center gap-2 text-sm text-slate-500"><ShieldCheck size={17} className="text-emerald-600"/> Secure reservation workflow</div></div>
      {isLoading ? <div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{[1,2,3].map(i => <div key={i} className="h-96 animate-pulse rounded-3xl bg-white shadow-sm"/>)}</div> : isError ? <div className="rounded-3xl border border-red-100 bg-white p-10 text-center"><p className="font-bold text-slate-900">Hotels are temporarily unavailable.</p><p className="mt-2 text-sm text-slate-500">Please try again in a moment.</p></div> : filtered.length === 0 ? <div className="rounded-3xl border border-dashed border-slate-300 bg-white p-14 text-center"><Building2 className="mx-auto text-slate-400" size={42}/><p className="mt-4 font-bold text-slate-900">No matching hotels</p><p className="mt-1 text-sm text-slate-500">Try another city, county or hotel name.</p></div> : <div className="grid gap-7 md:grid-cols-2 lg:grid-cols-3">{filtered.map(hotel => <article key={hotel._id} className="group overflow-hidden rounded-3xl bg-white shadow-sm ring-1 ring-slate-200/70 transition duration-300 hover:-translate-y-1 hover:shadow-xl"><div className="relative h-56 overflow-hidden bg-slate-100">{hotel.images?.[0] ? <img src={hotel.images[0]} alt={hotel.name} className="h-full w-full object-cover transition duration-500 group-hover:scale-105"/> : <div className="flex h-full items-center justify-center text-slate-300"><Building2 size={56}/></div>}<div className="absolute inset-x-0 top-0 flex justify-between p-4"><span className="rounded-full bg-slate-950/75 px-3 py-1 text-xs font-bold text-white backdrop-blur">{hotel.city || hotel.county || "Kenya"}</span>{hotel.starRating ? <span className="flex items-center gap-1 rounded-full bg-white px-2.5 py-1 text-xs font-black text-slate-800 shadow"><Star size={13} className="fill-current text-amber-500"/>{hotel.starRating}</span> : null}</div></div><div className="p-6"><h3 className="text-xl font-black text-slate-900">{hotel.name}</h3><p className="mt-2 flex items-center gap-1.5 text-sm text-slate-500"><MapPin size={15} className="text-emerald-600"/>{hotel.location || hotel.city || hotel.county || "Kenya"}</p><p className="mt-4 line-clamp-2 text-sm leading-6 text-slate-600">{hotel.description || "Comfortable accommodation for your Kenyan itinerary."}</p><div className="mt-5 space-y-3">{(hotel.roomTypes || []).map(room => <div key={room._id} className="rounded-2xl border border-slate-200 p-4 transition hover:border-emerald-200 hover:bg-emerald-50/30"><div className="flex items-start justify-between gap-3"><div><p className="font-bold text-slate-900">{room.name}</p><p className="mt-1 text-xs text-slate-500"><BedDouble size={13} className="mr-1 inline"/>Up to {room.maxAdults} adults · {room.availableRooms ?? "—"} available</p></div><p className="whitespace-nowrap text-right font-black text-slate-900">KES {Number(room.nightlyRate || 0).toLocaleString()}<span className="block text-[11px] font-medium text-slate-400">per night</span></p></div><button type="button" onClick={() => choose(hotel, room)} className="mt-3 w-full rounded-xl bg-slate-950 px-4 py-2.5 text-sm font-bold text-white transition hover:bg-emerald-600">Select room</button></div>)}</div></div></article>)}</div>}
    </section>
    {selected && <div className="fixed inset-0 z-[60] overflow-y-auto bg-slate-950/70 p-4 backdrop-blur-sm"><div className="mx-auto my-6 max-w-3xl overflow-hidden rounded-3xl bg-white shadow-2xl"><div className="flex items-start justify-between bg-slate-950 px-6 py-6 text-white"><div><p className="text-xs font-bold uppercase tracking-wider text-emerald-300">Reservation request</p><h2 className="mt-1 text-2xl font-black">{selected.hotel.name}</h2><p className="mt-1 text-sm text-slate-300">{selected.room.name} · KES {Number(selected.room.nightlyRate || 0).toLocaleString()} / night</p></div><button type="button" onClick={() => setSelected(null)} className="rounded-xl p-2 text-slate-300 hover:bg-white/10 hover:text-white" aria-label="Close"><X/></button></div><div className="p-6"><div className="mb-6 grid grid-cols-2 gap-3 rounded-2xl bg-emerald-50 p-4 text-sm text-emerald-900"><div className="flex items-center gap-2"><CalendarDays size={17}/><span>Choose your stay dates</span></div><div className="flex items-center justify-end gap-2"><Users size={17}/><span>Guest details</span></div></div><div className="grid gap-4 md:grid-cols-2">{field("Check-in", <input className={input} type="date" value={form.checkIn} onChange={e => update("checkIn", e.target.value)} />)}{field("Check-out", <input className={input} type="date" value={form.checkOut} onChange={e => update("checkOut", e.target.value)} />)}{field("Rooms", <input className={input} type="number" min="1" value={form.rooms} onChange={e => update("rooms", e.target.value)} />)}{field("Adults", <input className={input} type="number" min="1" max={selected.room.maxAdults} value={form.adults} onChange={e => update("adults", e.target.value)} />)}{field("Children", <input className={input} type="number" min="0" value={form.children} onChange={e => update("children", e.target.value)} />)}{field("Meal plan", <select className={input} value={form.mealPlan} onChange={e => update("mealPlan", e.target.value)}>{(selected.room.mealPlans?.length ? selected.room.mealPlans : ["room_only"]).map(x => <option key={x} value={x}>{String(x).replaceAll("_", " ")}</option>)}</select>)}{field("First name", <input className={input} value={form.firstName} onChange={e => update("firstName", e.target.value)} />)}{field("Last name", <input className={input} value={form.lastName} onChange={e => update("lastName", e.target.value)} />)}{field("Phone", <input className={input} inputMode="tel" placeholder="07xx xxx xxx" value={form.phone} onChange={e => update("phone", e.target.value)} />)}{field("Email", <input className={input} type="email" value={form.email} onChange={e => update("email", e.target.value)} />)}{field("Special requests", <textarea className={`${input} md:col-span-2`} rows="3" value={form.specialRequests} onChange={e => update("specialRequests", e.target.value)} />)}</div><div className="mt-7 flex flex-col-reverse gap-3 border-t border-slate-100 pt-5 sm:flex-row sm:items-center sm:justify-between"><p className="flex items-center gap-2 text-xs text-slate-500"><Check size={15} className="text-emerald-600"/>You will sign in before the reservation is submitted.</p><div className="flex gap-2"><Link to="/login" className="rounded-xl border border-slate-200 px-4 py-3 text-sm font-bold text-slate-700">Sign in</Link><button type="button" disabled={booking.isPending} onClick={() => booking.mutate({...form, hotelId: selected.hotel._id, roomTypeId: selected.room._id})} className="rounded-xl bg-emerald-600 px-5 py-3 text-sm font-black text-white shadow-lg shadow-emerald-600/20 hover:bg-emerald-500 disabled:opacity-50">{booking.isPending ? "Submitting…" : "Request reservation"}</button></div></div></div></div></div>}
  </main>;
}

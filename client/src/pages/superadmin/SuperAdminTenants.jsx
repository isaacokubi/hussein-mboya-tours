import { useState } from "react";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { toast } from "react-toastify";
import { deleteSuperAdminTenant, getSuperAdminTenants, updateSuperAdminTenantStatus } from "../../api/superAdminTenantsApi";
import { activateTenantSubscription } from "../../api/tenantSubscriptionApi";
const PLATFORM_HOST = String(import.meta.env.VITE_PLATFORM_HOST || "globaltours.com").replace(/^https?:\/\//, "").replace(/\/$/, ""); const tenantUrl = (tenant) => `https://${tenant.slug}.${PLATFORM_HOST}`;
export default function SuperAdminTenants() {
  const qc = useQueryClient(); const [search, setSearch] = useState(""); const [deleteTarget, setDeleteTarget] = useState(null); const [confirmation, setConfirmation] = useState(""); const [activateTarget, setActivateTarget] = useState(null); const [activation, setActivation] = useState({ plan: "starter", periodDays: 30, amount: "", reference: "" });
  const { data, isLoading, isError, error } = useQuery({ queryKey: ["platform-tenants", search], queryFn: () => getSuperAdminTenants({ search, limit: 100 }), staleTime: 30000, refetchOnMount: "always" }); const tenants = data?.tenants || data?.data || [];
  const statusMutation = useMutation({ mutationFn: ({ id, status }) => updateSuperAdminTenantStatus(id, status), onSuccess: (result) => { qc.invalidateQueries({ queryKey: ["platform-tenants"] }); toast.success(result.message || "Company status updated."); }, onError: (err) => toast.error(err?.response?.data?.message || err.message || "Unable to update company.") });
  const activateMutation = useMutation({ mutationFn: ({ id, payload }) => activateTenantSubscription(id, payload), onSuccess: (result) => { setActivateTarget(null); qc.invalidateQueries({ queryKey: ["platform-tenants"] }); qc.invalidateQueries({ queryKey: ["superadmin-dashboard"] }); toast.success(result.message || "Subscription activated."); }, onError: (err) => toast.error(err?.response?.data?.message || err.message || "Unable to activate subscription.") });
  const deleteMutation = useMutation({ mutationFn: ({ id }) => deleteSuperAdminTenant(id, "DELETE"), onSuccess: (result) => { setDeleteTarget(null); setConfirmation(""); qc.invalidateQueries({ queryKey: ["platform-tenants"] }); qc.invalidateQueries({ queryKey: ["superadmin-dashboard"] }); toast.success(result.message || "Company deleted successfully."); }, onError: (err) => toast.error(err?.response?.data?.message || err.message || "Unable to delete company.") });
  return <main className="space-y-6"><header className="flex flex-col gap-4 md:flex-row md:items-center md:justify-between"><div><h1 className="text-3xl font-bold text-slate-900">Companies / Tenants</h1><p className="mt-1 text-slate-600">Platform-wide company management, subscriptions and billing lifecycle.</p></div><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search companies..." className="rounded-xl border bg-white px-4 py-3 shadow-sm" /></header>
    {isLoading && <div className="rounded-2xl border bg-white p-8">Loading companies...</div>}{isError && <div className="rounded-2xl border border-red-200 bg-red-50 p-6 text-red-700">{error?.response?.data?.message || error?.message || "Unable to load companies."}</div>}
    {!isLoading && !isError && <div className="overflow-x-auto rounded-2xl border bg-white shadow-sm"><table className="min-w-[1450px] w-full text-left"><thead className="border-b bg-slate-50"><tr>{["Company","Tenant URL","Owner","Status","Plan","Trial / Renewal","Users","Tours","Bookings","Created","Management"].map((label) => <th key={label} className="px-5 py-4 text-sm font-semibold text-slate-600">{label}</th>)}</tr></thead><tbody className="divide-y">{tenants.map((tenant) => { const c = tenant.counts || {}; const protectedTenant = tenant.slug === "platform" || tenant.isSystem; const sub = tenant.subscription || {}; const date = tenant.status === "trial" ? sub.trialEndsAt : sub.renewsAt; return <tr key={tenant._id} className="hover:bg-slate-50"><td className="px-5 py-4"><div className="font-bold">{tenant.name}</div><div className="text-xs text-slate-500">{tenant.slug}</div></td><td className="px-5 py-4"><a href={tenantUrl(tenant)} target="_blank" rel="noreferrer" className="text-xs font-medium text-emerald-700 hover:underline">{tenantUrl(tenant)}</a></td><td className="px-5 py-4">{tenant.owner ? <><div>{tenant.owner.name}</div><div className="text-xs text-slate-500">{tenant.owner.email}</div></> : <span className="text-slate-400">No owner</span>}</td><td className="px-5 py-4"><span className={`rounded-full px-3 py-1 text-xs font-semibold capitalize ${tenant.status === "active" ? "bg-emerald-100 text-emerald-800" : tenant.status === "trial" ? "bg-amber-100 text-amber-800" : "bg-slate-100 text-slate-700"}`}>{tenant.status}</span></td><td className="px-5 py-4 capitalize">{sub.plan || "—"}</td><td className="px-5 py-4 text-xs">{date ? new Date(date).toLocaleDateString() : "—"}</td><td className="px-5 py-4">{c.users || 0}</td><td className="px-5 py-4">{c.tours || c.tourpackages || 0}</td><td className="px-5 py-4">{c.bookings || 0}</td><td className="px-5 py-4 text-sm">{tenant.createdAt ? new Date(tenant.createdAt).toLocaleDateString() : "—"}</td><td className="px-5 py-4"><div className="flex flex-wrap gap-2"><button onClick={() => toast.info(`${tenant.name}: ${c.users || 0} users, ${c.tours || c.tourpackages || 0} tours, ${c.bookings || 0} bookings.`)} className="rounded-lg border px-3 py-2 text-sm font-medium">View</button><button disabled={protectedTenant || statusMutation.isPending} onClick={() => statusMutation.mutate({ id: tenant._id, status: tenant.status === "suspended" ? "active" : "suspended" })} className="rounded-lg bg-slate-900 px-3 py-2 text-sm font-medium text-white disabled:opacity-40">{tenant.status === "suspended" ? "Activate" : "Suspend"}</button>{!protectedTenant && <button onClick={() => { setActivateTarget(tenant); setActivation({ plan: tenant.subscription?.plan || "starter", periodDays: 30, amount: "", reference: "" }); }} className="rounded-lg bg-emerald-700 px-3 py-2 text-sm font-medium text-white">Paid / Activate</button>}<button disabled={protectedTenant} onClick={() => { setDeleteTarget(tenant); setConfirmation(""); }} className="rounded-lg bg-red-600 px-3 py-2 text-sm font-medium text-white disabled:opacity-40">Delete</button></div></td></tr>; })}</tbody></table>{tenants.length === 0 && <div className="p-10 text-center text-slate-500">No companies found.</div>}</div>}
    {activateTarget && <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/60 p-4"><div className="w-full max-w-lg rounded-2xl bg-white p-7 shadow-2xl"><h2 className="text-2xl font-bold">Activate {activateTarget.name} subscription</h2><p className="mt-2 text-sm text-slate-600">Use this for a verified bank/manual payment. M-Pesa payments activate automatically after a successful callback.</p><div className="mt-5 grid gap-3"><label className="text-sm font-semibold">Plan<select value={activation.plan} onChange={(e) => setActivation({ ...activation, plan: e.target.value })} className="mt-1 w-full rounded-xl border p-3"><option value="starter">Starter</option><option value="professional">Professional</option><option value="business">Business</option><option value="enterprise">Enterprise</option></select></label><label className="text-sm font-semibold">Paid days<input type="number" min="1" max="3660" value={activation.periodDays} onChange={(e) => setActivation({ ...activation, periodDays: e.target.value })} className="mt-1 w-full rounded-xl border p-3" /></label><label className="text-sm font-semibold">Amount<input type="number" min="1" value={activation.amount} onChange={(e) => setActivation({ ...activation, amount: e.target.value })} placeholder="Amount received" className="mt-1 w-full rounded-xl border p-3" /></label><label className="text-sm font-semibold">Payment reference<input value={activation.reference} onChange={(e) => setActivation({ ...activation, reference: e.target.value })} placeholder="M-Pesa receipt / bank reference" className="mt-1 w-full rounded-xl border p-3" /></label></div><div className="mt-5 flex justify-end gap-3"><button onClick={() => setActivateTarget(null)} className="rounded-lg border px-4 py-2">Cancel</button><button disabled={activateMutation.isPending || !activation.amount || !activation.reference} onClick={() => activateMutation.mutate({ id: activateTarget._id, payload: activation })} className="rounded-lg bg-emerald-700 px-4 py-2 font-semibold text-white disabled:opacity-40">{activateMutation.isPending ? "Activating..." : "Confirm paid & activate"}</button></div></div></div>}
    {deleteTarget && <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/60 p-4"><div className="w-full max-w-lg rounded-2xl bg-white p-7 shadow-2xl"><h2 className="text-2xl font-bold text-red-700">Delete {deleteTarget.name} permanently?</h2><p className="mt-3 text-slate-600">This permanently removes the company and tenant-scoped records. Other companies are not affected.</p><p className="mt-4 font-semibold">Type <code className="rounded bg-slate-100 px-2 py-1">DELETE</code> to confirm.</p><input autoFocus value={confirmation} onChange={(e) => setConfirmation(e.target.value)} className="mt-3 w-full rounded-xl border p-3" placeholder="DELETE" /><div className="mt-5 flex justify-end gap-3"><button onClick={() => setDeleteTarget(null)} className="rounded-lg border px-4 py-2">Cancel</button><button disabled={confirmation !== "DELETE" || deleteMutation.isPending} onClick={() => deleteMutation.mutate({ id: deleteTarget._id })} className="rounded-lg bg-red-600 px-4 py-2 font-semibold text-white disabled:opacity-40">{deleteMutation.isPending ? "Deleting..." : "Delete Permanently"}</button></div></div></div>}
  </main>;
}

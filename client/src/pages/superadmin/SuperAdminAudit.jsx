import { useState } from "react";
import { useQuery } from "@tanstack/react-query";
import { getAuditLogs } from "../../api/auditApi";
import Pagination from "../../components/admin/Pagination";

const PAGE_SIZE = 10;
export default function SuperAdminAudit() {
  const [page, setPage] = useState(1); const [selected, setSelected] = useState(null); const [filters, setFilters] = useState({ search: "", severity: "", status: "", action: "", resource: "" });
  const { data, isLoading, error, refetch } = useQuery({ queryKey: ["audit-center", page, filters], queryFn: () => getAuditLogs({ page, limit: PAGE_SIZE, ...filters }) });
  const logs = data?.logs || []; const pages = Math.max(1, Number(data?.pagination?.pages || 1)); const total = Number(data?.pagination?.total || data?.statistics?.total || logs.length);
  const changeFilter = (key, value) => { setFilters((current) => ({ ...current, [key]: value })); setPage(1); };
  if (isLoading) return <div className="min-h-screen bg-slate-50 p-8 text-slate-600">Loading audit activity...</div>;
  if (error) return <div className="min-h-screen bg-slate-50 p-8"><div className="rounded-2xl border border-red-200 bg-red-50 p-6 text-red-700">Failed to load audit logs.<button type="button" onClick={() => refetch()} className="ml-4 rounded-lg bg-red-700 px-3 py-2 text-white">Retry</button></div></div>;
  return <div className="min-h-screen space-y-6 bg-slate-50 p-4 sm:p-8"><header><h1 className="text-3xl font-bold text-slate-900">Audit Center</h1><p className="text-gray-500">Platform activity monitoring and security events</p></header><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><Card title="Total Events" value={data?.statistics?.total || 0}/><Card title="Successful" value={data?.statistics?.success || 0}/><Card title="Failed" value={data?.statistics?.failed || 0}/><Card title="Critical" value={data?.statistics?.critical || 0}/></div><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-5"><input className="rounded-xl border p-3" placeholder="Search" value={filters.search} onChange={(e) => changeFilter("search", e.target.value)} /><select className="rounded-xl border p-3" value={filters.action} onChange={(e) => changeFilter("action", e.target.value)}><option value="">Action</option><option value="login">Login</option><option value="create">Create</option><option value="update">Update</option><option value="delete">Delete</option></select><select className="rounded-xl border p-3" value={filters.severity} onChange={(e) => changeFilter("severity", e.target.value)}><option value="">Severity</option><option value="critical">Critical</option><option value="high">High</option><option value="medium">Medium</option><option value="low">Low</option></select><select className="rounded-xl border p-3" value={filters.status} onChange={(e) => changeFilter("status", e.target.value)}><option value="">Status</option><option value="success">Success</option><option value="failed">Failed</option></select><button className="rounded-xl border bg-white px-4 font-semibold" onClick={() => refetch()}>Refresh</button></div><div className="overflow-auto rounded-2xl bg-white shadow-sm ring-1 ring-slate-200"><table className="w-full min-w-[950px]"><thead className="bg-slate-50"><tr>{["User","Action","Resource","Description","Status","Severity","Date"].map((h) => <th key={h} className="p-3 text-left text-xs uppercase text-slate-500">{h}</th>)}</tr></thead><tbody>{logs.length === 0 ? <tr><td colSpan="7" className="p-8 text-center text-gray-500">No audit events found.</td></tr> : logs.map((log) => <tr key={log._id} className="cursor-pointer border-t hover:bg-emerald-50/40" onClick={() => setSelected(log)}><td className="p-3">{log.user?.name || "System"}</td><td>{log.action}</td><td>{log.resource}</td><td>{log.description || "-"}</td><td><Badge value={log.status}/></td><td><Badge value={log.severity}/></td><td>{new Date(log.createdAt).toLocaleString("en-KE")}</td></tr>)}</tbody></table><Pagination page={page} pages={pages} total={total} pageSize={PAGE_SIZE} onPageChange={setPage}/></div>{selected && <div className="fixed inset-0 z-50 flex justify-end bg-black/40" onClick={() => setSelected(null)}><div className="h-full w-full max-w-lg overflow-auto bg-white p-6" onClick={(e) => e.stopPropagation()}><h2 className="text-xl font-bold mb-4">Audit Details</h2><pre className="whitespace-pre-wrap text-sm">{JSON.stringify(selected, null, 2)}</pre><button className="mt-5 rounded-lg border px-4 py-2" onClick={() => setSelected(null)}>Close</button></div></div>}</div>;
}
function Card({ title, value }) { return <div className="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><p className="text-sm text-gray-500">{title}</p><h2 className="text-3xl font-bold text-slate-900">{Number(value || 0).toLocaleString("en-KE")}</h2></div>; }
function Badge({ value }) { const status = String(value || "").toLowerCase(); const classes = status === "success" || status === "active" ? "text-green-700 bg-green-100" : status === "failed" || status === "critical" ? "text-red-700 bg-red-100" : status === "high" ? "text-orange-700 bg-orange-100" : "text-gray-700 bg-gray-100"; return <span className={`rounded-full px-3 py-1 text-xs font-semibold ${classes}`}>{value || "unknown"}</span>; }

HUSSEIN MBOYA TOURS - TOUR CONTROLLER REPAIR REPORT
======================================================

Files replaced:
- server/controllers/tourController.js
- server/controllers/tourManagerController.js
- server/controllers/adminTourController.js
- server/controllers/tourAssignmentController.js
- server/routes/tourRoutes.js

File added:
- server/services/tourResourceService.js

Main repairs:
1. Removed competing/duplicated tour CRUD implementations.
2. Standardized guide and driver assignments on Staff.
3. Standardized vehicle assignments on Vehicle.
4. Standardized vehicle status values to the schema: available/assigned.
5. Added safe release of old guide, driver and vehicle assignments.
6. Preserved assignedTours and staff availability correctly.
7. Prevented assignment of inactive/deleted resources.
8. Added canonical assignment behavior for guide/driver/vehicle.
9. Preserved frontend compatibility endpoints.
10. Fixed public tour lookup so deleted/unpublished tours are not exposed.
11. Fixed /api/tours/manager route ordering so it is not swallowed by /:id.
12. Restricted tour updates to explicit allowed fields.
13. Added validation for capacity, price and discount.
14. Fixed soft-delete resource cleanup.
15. Fixed restore behavior so it does not incorrectly mark unrelated resources busy.
16. Preserved response aliases such as data/tour/tours for frontend compatibility.
17. Kept admin, manager and public controllers compatible with the existing routes.

Validation:
- node --check passed for every JavaScript file in server/.
- Modified controller files passed syntax validation.
- Full runtime import could not be executed in the sandbox because npm dependencies
  (including mongoose) are not installed in the uploaded ZIP. Run npm install in
  your local server directory before runtime testing.

Recommended merge procedure:
1. Back up the current project.
2. Extract this ZIP over a copy of the project, or copy the listed files.
3. In server/: npm install
4. Run: find . -name "*.js" -not -path "./node_modules/*" -exec node --check {} \;
5. Start backend: npm run dev
6. Test:
   PUT /api/tour-assignments/:id/assign
   Body can contain guideId, driverId, vehicleId.
   Undefined fields remain unchanged; null/empty fields clear the assignment.
7. Test manager route:
   GET /api/tours/manager
8. Test admin assignment routes:
   PATCH /api/admin/tours/:id/guide
   PATCH /api/admin/tours/:id/driver
   PATCH /api/admin/tours/:id/vehicle
